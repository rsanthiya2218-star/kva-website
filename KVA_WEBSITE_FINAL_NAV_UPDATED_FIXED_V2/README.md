# KVA Website

The project is separated into `frontend` and `backend` folders.

## Run Frontend

```bash
cd frontend
npm install
npm run dev
```

The Vite entry file is:
`frontend/src/main.jsx`

## Run Backend

```bash
cd backend
npm install
npm start
```

Do not run `npm run dev` from the parent folder. Run it from `frontend`.
