# KVA Website Frontend

## Run

```bash
npm install
npm run dev
```

For local development, Vite proxies `/api` to `http://localhost:3001`.
For a separately hosted backend, set `VITE_API_BASE_URL` in `.env`.
